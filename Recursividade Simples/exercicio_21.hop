! Elaborar função chamada contaDigitos(n) que retorne a quantidade de dígitos de um valor numérico fornecido.

dec contaDigitos : num -> num;
--- contaDigitos 0 <= 0;
--- contaDigitos n <= 1 + contaDigitos( n div 10);
contaDigitos 123;
